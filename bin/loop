#!/bin/sh
#
# Laptop nice and toasty by 10,000,000 iterations,
# guaranteed.

#i=0
#while true; do
#    echo $i
#    i=$((i+1))
#done

dd if=/dev/zero of=/dev/null | dd if=/dev/zero of=/dev/null | dd if=/dev/zero of=/dev/null | dd if=/dev/zero of=/dev/null
